# Variables d'environnement pour le frontend MandeMarket
# Copier ce fichier en .env.local pour l'utiliser

# URL de l'API backend (navigateur)
NEXT_PUBLIC_API_URL=http://localhost:4002

# URL interne API pour SSR sous Docker (cf. frontend/.env.docker)
# INTERNAL_API_URL=http://backend:4002

# URL du site (pour les redirections, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Environnement
NODE_ENV=development

# Configuration Stripe (quand implémenté)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Configuration upload d'images (quand implémenté)
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Analytics (quand implémenté)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-...
